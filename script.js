function hero(newColor) {
    const elem = document.getElementById('abcd');
    elem.style.color = newColor;
  }
  function ero(newColor) {
    const elem = document.getElementById('abcd');
    elem.style.backgroundColor = newColor;
  }.
